# Покрокова інструкція: Деплой на GitHub Pages

## Крок 1: Підготовка проекту

### 1.1 Перевірте файли
Переконайтеся, що всі файли на місці:
- `index.html`
- `term.html`
- `css/styles.css`
- `js/config.js`
- `js/sheets-api.js`
- `js/search.js`
- `js/app.js`
- `images/logo.svg` (якщо є)

### 1.2 Перевірте конфігурацію
Відкрийте `js/config.js` і переконайтеся, що всі налаштування правильні:
- ✅ API_KEY встановлено
- ✅ SHEET_ID встановлено
- ✅ TERMS_SHEET встановлено
- ✅ URLs форм встановлено

## Крок 2: Створення GitHub репозиторію

### 2.1 Створіть новий репозиторій
1. Перейдіть на [GitHub.com](https://github.com)
2. Натисніть кнопку **"+"** в правому верхньому куті → **"New repository"**
3. Заповніть форму:
   - **Repository name:** `eu4pfm-tax-glossary` (або інша назва)
   - **Description:** "Словник податкових термінів EN-UK"
   - **Visibility:** Public (GitHub Pages безкоштовно тільки для публічних репозиторіїв)
   - **НЕ** додавайте README, .gitignore або license (ми вже маємо файли)
4. Натисніть **"Create repository"**

## Крок 3: Завантаження файлів на GitHub

### Варіант A: Через GitHub веб-інтерфейс (найпростіше)

1. Після створення репозиторію ви побачите сторінку з інструкціями
2. Натисніть **"uploading an existing file"**
3. Перетягніть всі файли проекту в браузер:
   - `index.html`
   - `term.html`
   - Папку `css/` з файлом `styles.css`
   - Папку `js/` з усіма JS файлами
   - Папку `images/` з логотипом (якщо є)
   - `README.md` (якщо хочете)
4. Прокрутіть вниз, введіть повідомлення коміту: `"Initial commit: Add glossary project"`
5. Натисніть **"Commit changes"**

### Варіант B: Через Git командний рядок (для досвідчених)

```bash
# Перейдіть в папку проекту
cd "/Users/ppolikar/Documents/Cursor/eu4pfm TAX EN-UK Glossary"

# Ініціалізуйте Git репозиторій
git init

# Додайте всі файли
git add .

# Створіть перший коміт
git commit -m "Initial commit: Add glossary project"

# Додайте віддалений репозиторій (замініть YOUR_USERNAME та REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Завантажте файли на GitHub
git branch -M main
git push -u origin main
```

## Крок 4: Налаштування GitHub Pages

1. У вашому репозиторії на GitHub перейдіть до **Settings** (вкладка вгорі)
2. У лівому меню знайдіть **Pages** (в розділі "Code and automation")
3. У розділі **"Source"**:
   - Виберіть гілку: **`main`** (або `master`, якщо використовуєте стару назву)
   - Виберіть папку: **`/ (root)`**
4. Натисніть **"Save"**

## Крок 5: Очікування активації

1. Після збереження налаштувань GitHub почне деплой
2. Зачекайте 1-2 хвилини
3. Оновіть сторінку Settings → Pages
4. Ви побачите повідомлення: **"Your site is live at..."**
5. URL буде виглядати так: `https://YOUR_USERNAME.github.io/REPO_NAME/`

## Крок 6: Перевірка сайту

1. Відкрийте URL вашого сайту в браузері
2. Перевірте, що:
   - ✅ Сторінка завантажується
   - ✅ Дані завантажуються з Google Sheets
   - ✅ Пошук працює
   - ✅ Форми відкриваються
   - ✅ Логотип відображається

## Крок 7: Оновлення сайту (якщо потрібно внести зміни)

### Через веб-інтерфейс:
1. Перейдіть до файлу, який потрібно змінити
2. Натисніть іконку олівця (Edit)
3. Внесіть зміни
4. Прокрутіть вниз, введіть повідомлення коміту
5. Натисніть **"Commit changes"**
6. Зачекайте 1-2 хвилини - сайт оновиться автоматично

### Через Git:
```bash
# Внесіть зміни в файли
# Потім:
git add .
git commit -m "Опис змін"
git push
```

## Важливі примітки

### ⚠️ Безпека API ключа

Ваш API ключ буде видимий у публічному репозиторії. Це нормально, якщо:
- ✅ API ключ обмежений до Google Sheets API
- ✅ API ключ обмежений до конкретної таблиці (якщо можливо)
- ✅ Таблиця має публічний доступ (тільки читання)

**НЕ** використовуйте API ключі з повними правами доступу!

### 🔄 Автоматичне оновлення

GitHub Pages автоматично оновлює сайт після кожного коміту. Зазвичай це займає 1-2 хвилини.

### 📱 Кастомний домен (опціонально)

Якщо хочете використовувати власний домен:
1. У Settings → Pages додайте ваш домен
2. Налаштуйте DNS записи у вашому провайдері домену

## Вирішення проблем

### Сайт не завантажується
- Перевірте, що всі файли завантажені
- Перевірте, що вибрано правильну гілку (`main`)
- Зачекайте 5-10 хвилин (іноді потрібно більше часу)

### Дані не завантажуються
- Перевірте, що API ключ правильний
- Перевірте, що таблиця має публічний доступ
- Відкрийте консоль браузера (F12) і перевірте помилки

### Форми не працюють
- Перевірте, що URLs форм правильні
- Перевірте, що форми мають публічний доступ
- Перевірте, що використано правильний формат URL (`/viewform?embedded=true`)

## Готово! 🎉

Ваш сайт тепер доступний за адресою:
`https://YOUR_USERNAME.github.io/REPO_NAME/`

Поділіться цим посиланням з користувачами!

