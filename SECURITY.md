# 🔒 Інструкція з безпеки

## ⚠️ Важливо про API ключ

API ключ Google Sheets **видимий у коді** - це нормально для статичних сайтів, оскільки весь JavaScript виконується в браузері користувача. Однак **обов'язково** налаштуйте обмеження для захисту!

## 🛡️ Налаштування обмежень API ключа

### Крок 1: Обмеження по API
1. Відкрийте [Google Cloud Console](https://console.cloud.google.com/)
2. Перейдіть до **APIs & Services** → **Credentials**
3. Знайдіть ваш API ключ (`AIzaSyD33zzQZ438Kjsv9UqSoxi0yeBxYoyLX8E`)
4. Натисніть на ключ для редагування
5. У розділі **"API restrictions"**:
   - Виберіть **"Restrict key"**
   - Оберіть **тільки "Google Sheets API"**
   - Натисніть **SAVE**

### Крок 2: Обмеження по HTTP referrer (рекомендовано)
1. У тому ж вікні редагування ключа
2. У розділі **"Application restrictions"**:
   - Виберіть **"HTTP referrers (web sites)"**
   - Додайте наступні домени:
     ```
     https://ppolikar.github.io/*
     https://*.github.io/*
     http://localhost:8000/*
     http://127.0.0.1:8000/*
     ```
   - Натисніть **SAVE**

### Крок 3: Перевірка
- Після збереження ключ працюватиме **тільки**:
  - З домену GitHub Pages
  - З локального сервера (для розробки)
  - Тільки для Google Sheets API

## 📝 Що означає "користувачі не бачать код"

Для статичних сайтів (HTML/CSS/JS) **неможливо повністю приховати код**, оскіль що:
- Весь JavaScript завантажується в браузер користувача
- Користувач може переглянути код через DevTools
- Це нормальна поведінка для веб-сайтів

### Що можна зробити:
1. ✅ **Мінімізувати код** - зробити його менш читабельним (але не приховати)
2. ✅ **Обмежити API ключ** - найважливіше! (див. вище)
3. ✅ **Використовувати обмежені API ключі** - тільки для потрібних API

### Що НЕ можна зробити:
- ❌ Повністю приховати JavaScript код
- ❌ Заборонити перегляд коду через DevTools
- ❌ Захистити API ключ від перегляду (він має бути в коді)

## 🔐 Рекомендації

1. **Обмежте API ключ** - найважливіше!
2. **Не використовуйте один ключ для кількох проектів**
3. **Регулярно перевіряйте використання ключа** в Google Cloud Console
4. **Якщо ключ скомпрометовано** - видаліть його та створіть новий

## 📊 Моніторинг використання

1. Відкрийте [Google Cloud Console](https://console.cloud.google.com/)
2. Перейдіть до **APIs & Services** → **Dashboard**
3. Перегляньте статистику використання API
4. Якщо бачите підозрілу активність - негайно обмежте або видаліть ключ

## ⚡ Швидка перевірка безпеки

- [ ] API ключ обмежений тільки до Google Sheets API
- [ ] Додано обмеження по HTTP referrer
- [ ] Немає інших конфіденційних даних в коді
- [ ] Google Sheets таблиця має правильні налаштування доступу

## 🆘 Якщо ключ скомпрометовано

1. **Негайно видаліть ключ** в Google Cloud Console
2. Створіть новий API ключ
3. Оновіть `js/config.js` з новим ключем
4. Закомітьте зміни та задеплойте на GitHub Pages

