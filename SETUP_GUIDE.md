# Детальна інструкція: Налаштування Google Sheets API

## Крок 1: Вхід до Google Cloud Console

1. Відкрийте браузер і перейдіть на: **https://console.cloud.google.com/**
2. Увійдіть у свій Google акаунт (той самий, що має доступ до вашої Google таблиці)
3. Якщо це ваш перший раз, прийміть умови використання

---

## Крок 2: Створення або вибір проекту

### Варіант A: Створення нового проекту (рекомендовано)

1. У верхній частині сторінки натисніть на **випадаючий список з назвою проекту** (зазвичай там написано "Select a project" або назва існуючого проекту)
2. Натисніть кнопку **"NEW PROJECT"** (або "Створити проект")
3. Введіть назву проекту, наприклад: **"Tax Glossary API"**
4. Натисніть **"CREATE"** (або "Створити")
5. Зачекайте 10-30 секунд, поки проект створиться
6. Після створення проект автоматично стане активним

### Варіант B: Використання існуючого проекту

1. У верхній частині сторінки натисніть на **випадаючий список з назвою проекту**
2. Знайдіть потрібний проект у списку
3. Натисніть на нього, щоб зробити активним

---

## Крок 3: Увімкнення Google Sheets API

1. У лівому меню натисніть на **"APIs & Services"** (або "API та сервіси")
2. Виберіть **"Library"** (або "Бібліотека")
3. У полі пошуку введіть: **"Google Sheets API"**
4. Натисніть на результат **"Google Sheets API"**
5. На сторінці API натисніть велику синю кнопку **"ENABLE"** (або "Увімкнути")
6. Зачекайте 10-20 секунд, поки API увімкнеться
7. Після увімкнення ви побачите повідомлення "API enabled" (API увімкнено)

---

## Крок 4: Створення API ключа

1. У лівому меню натисніть на **"APIs & Services"** → **"Credentials"** (або "API та сервіси" → "Облікові дані")
2. У верхній частині сторінки натисніть кнопку **"+ CREATE CREDENTIALS"** (або "+ Створити облікові дані")
3. У випадаючому меню виберіть **"API key"** (або "Ключ API")
4. З'явиться діалогове вікно **"Create API key"**

### Що робити в діалоговому вікні:

**4.1. Назва ключа (опціонально):**
   - Можна залишити "API key 1" або ввести свою назву (наприклад, "Tax Glossary API Key")
   - Це просто для ідентифікації, не впливає на функціонал

**4.2. Service Account (не потрібно):**
   - **НЕ встановлюйте** галочку "Authenticate API calls through a service account"
   - Це для складніших випадків, нам не потрібно

**4.3. Обмеження API (ВАЖЛИВО!):**
   - У розділі **"API restrictions"** виберіть **"Restrict key"** (замість "Don't restrict key")
   - Після вибору з'явиться список API
   - Знайдіть та виберіть **"Google Sheets API"**
   - Переконайтеся, що інші API не вибрані

**4.4. Application restrictions (опціонально):**
   - Можна залишити **"None"** для тестування
   - Або вибрати **"Websites"** і додати `localhost:8000/*` для локального тестування

**4.5. Створення:**
   - Натисніть синю кнопку **"Create"** внизу вікна
   - Після створення з'явиться нове вікно з вашим API ключем

**4.6. Копіювання ключа:**
   - **ВАЖЛИВО:** Скопіюйте ключ одразу! Він виглядає приблизно так:
     ```
     AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
     ```
   - Вставте його в `js/config.js` замість `YOUR_API_KEY_HERE`
   - Натисніть **"CLOSE"** (або "Закрити") у вікні

**Примітка:** Якщо ви забули обмежити ключ під час створення, не хвилюйтеся - можна зробити це пізніше (див. Крок 5)

---

## Крок 5: Обмеження API ключа (рекомендовано для безпеки)

### 5.1. Обмеження за типом API

1. У списку "API keys" знайдіть щойно створений ключ
2. Натисніть на **назву ключа** (або на іконку олівця для редагування)
3. У розділі **"API restrictions"** (Обмеження API):
   - Виберіть **"Restrict key"** (Обмежити ключ)
   - У списку "Select APIs" виберіть **"Google Sheets API"**
   - Зніміть галочки з інших API (якщо вони є)
4. У розділі **"Application restrictions"** (Обмеження додатків):
   - Можна залишити **"None"** для тестування
   - Або вибрати **"HTTP referrers"** і додати ваші домени (наприклад, `localhost:8000/*` для локального тестування)
5. Натисніть **"SAVE"** (або "Зберегти") внизу сторінки
6. Зачекайте кілька секунд, поки зміни збережуться

### 5.2. Альтернативний спосіб обмеження (через меню)

1. У списку "API keys" натисніть на **три крапки (⋮)** біля вашого ключа
2. Виберіть **"Edit"** (Редагувати)
3. Виконайте кроки з пункту 5.1

---

## Крок 6: Отримання Sheet ID з Google таблиці

1. Відкрийте вашу Google таблицю в браузері
2. Подивіться на URL у адресному рядку браузера
3. URL виглядає так:
   ```
   https://docs.google.com/spreadsheets/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit
   ```
4. **Sheet ID** — це частина між `/d/` та `/edit`:
   ```
   1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms
   ```
5. Скопіюйте цей ID

---

## Крок 7: Налаштування config.js

1. Відкрийте файл `js/config.js` у вашому проекті
2. Знайдіть рядки:
   ```javascript
   API_KEY: 'YOUR_API_KEY_HERE',
   SHEET_ID: 'YOUR_SHEET_ID_HERE',
   ```
3. Замініть `YOUR_API_KEY_HERE` на ваш API ключ (з Кроку 4)
4. Замініть `YOUR_SHEET_ID_HERE` на ваш Sheet ID (з Кроку 6)
5. Перевірте назву листа:
   ```javascript
   TERMS_SHEET: 'Terms', // Якщо ваш лист називається інакше, змініть тут
   ```
6. Збережіть файл

**Приклад після налаштування:**
```javascript
const CONFIG = {
    API_KEY: 'AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx',
    SHEET_ID: '1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms',
    TERMS_SHEET: 'Terms',
    // ...
};
```

---

## Крок 8: Перевірка доступу до таблиці

### Варіант A: Публічний доступ (найпростіше)

1. Відкрийте вашу Google таблицю
2. Натисніть кнопку **"Share"** (Поділитися) у правому верхньому куті
3. У розділі "Get link" (Отримати посилання):
   - Натисніть **"Change to anyone with the link"** (Змінити на "Будь-хто з посиланням")
   - Виберіть **"Viewer"** (Переглядач) — тільки читання
4. Натисніть **"Done"** (Готово)

### Варіант B: Доступ через Service Account (складніше, але безпечніше)

Якщо ви хочете залишити таблицю приватною, потрібно використати Service Account. Це складніше, тому для тимчасового продукту рекомендую Варіант A.

---

## Крок 9: Тестування

1. Переконайтеся, що локальний сервер запущено:
   ```bash
   python3 -m http.server 8000
   ```
2. Відкрийте браузер і перейдіть на: **http://localhost:8000**
3. Перевірте консоль браузера (F12 → Console):
   - Якщо бачите помилки CORS — переконайтеся, що використовуєте локальний сервер
   - Якщо бачите помилки API — перевірте API ключ та Sheet ID
4. Якщо все працює, ви побачите список термінів!

---

## Типові помилки та рішення

### Помилка: "API key not valid"
- **Причина:** Неправильний API ключ або ключ не обмежений до Google Sheets API
- **Рішення:** Перевірте API ключ у `config.js` та обмеження в Google Cloud Console

### Помилка: "The caller does not have permission"
- **Причина:** Таблиця приватна або API ключ не має доступу
- **Рішення:** Зробіть таблицю публічною (Крок 8, Варіант A) або надайте доступ Service Account

### Помилка: "Unable to parse range"
- **Причина:** Неправильна назва листа або лист не існує
- **Рішення:** Перевірте назву листа в `config.js` (поле `TERMS_SHEET`)

### Помилка: CORS
- **Причина:** Відкриваєте `index.html` напряму, без локального сервера
- **Рішення:** Використовуйте локальний сервер (`python3 -m http.server 8000`)

### Помилка: "Request had insufficient authentication scopes"
- **Причина:** Google Sheets API не увімкнено для проекту
- **Рішення:** Поверніться до Кроку 3 і переконайтеся, що API увімкнено

---

## Додаткові поради

1. **Безпека API ключа:**
   - Ніколи не комітьте API ключ у публічний GitHub репозиторій
   - Обмежте ключ до Google Sheets API
   - Для продакшену розгляньте використання Service Account

2. **Ліміти API:**
   - Google Sheets API має ліміт: 100 запитів на 100 секунд на користувача
   - Для тимчасового продукту це більш ніж достатньо
   - Кешування в коді допомагає зменшити кількість запитів

3. **Назви колонок:**
   - Переконайтеся, що перший рядок таблиці містить правильні назви колонок
   - Назви повинні точно відповідати: `Status`, `Term`, `Definition`, `Notes`, `Абревіатура`, `Термін`, `Визначення`, `Примітки`

---

## Швидка перевірка чеклист

- [ ] Проект створено в Google Cloud Console
- [ ] Google Sheets API увімкнено
- [ ] API ключ створено та скопійовано
- [ ] API ключ обмежено до Google Sheets API
- [ ] Sheet ID отримано з URL таблиці
- [ ] `config.js` налаштовано з правильними значеннями
- [ ] Таблиця має публічний доступ (або налаштовано Service Account)
- [ ] Локальний сервер запущено
- [ ] Сайт відкривається без помилок у консолі

---

## Підтримка

Якщо виникли проблеми:
1. Перевірте консоль браузера (F12 → Console)
2. Перевірте Network вкладку (F12 → Network) для перегляду запитів до API
3. Переконайтеся, що всі кроки виконано правильно
4. Перевірте, що таблиця має правильну структуру

**Успіхів! 🚀**

